
# processed.deep_merge({
#   extracted: {
#     :title => "commented on pull request code: #{original_hash['payload']['comment']['path']}",
#     :url => original_hash['payload']['comment']['_links']['html']['href'],
#       :body => original_hash['payload']['comment']['body'],
#   }
# })

# commit comment
