<div class="panel panel-default">
  <div class="panel-heading">
	Choose brand
  </div>
  <div class="panel-body">
	<% @brands.each do |b| %>
		<p><%= button_to b.name, choose_brand_path(brand_id: b.id) %></p>
	<% end %>
  </div>
</div>
