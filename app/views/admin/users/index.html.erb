<h1>Users</h1>
 
<table class="table table-stripped table-hover">
  <tr>
    <th>Name</th>
    <th>Email</th>
  </tr>
 
<% @users.each do |u| %>
  <tr>
    <td><%= link_to u.name, admin_user_path(u) %></td>
    <td><%= u.email %></td>
  </tr>
<% end %>
</table>

<%= paginate @users %>
