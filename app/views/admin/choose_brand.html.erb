<div class="panel panel-default">
  <div class="panel-heading">
	Choose brand
  </div>
  <div class="panel-body">
	<% @brands.each do |brand| %>
	<p><%= link_to brand.name, admin_choose_brand_path(tenant_name: brand.tenant_name) %></p>
	<% end %>
  </div>
</div>
