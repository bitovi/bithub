json.cache! ("v1" + list_cache_key(@events)) do
  json.set! :data do 
    json.array! @events do |event|
      json.partial! "api/v1/events/event", event: event, second_level: false
    end
  end
end
