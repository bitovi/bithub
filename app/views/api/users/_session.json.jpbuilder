json.(user, :id, :name, :email, :address, :city, :postal, :state, :country, :activities, :achievements, :identities, :roles)

json.upvoted_events user.upvotes.map {|u| u.applies_to_id}
  
json.score user.score
json.avatar_url user.props['avatar_url']
