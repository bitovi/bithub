<div class="panel panel-default">
  <div class="panel-heading">
	Choose organization
  </div>
  <div class="panel-body">
	<% @organizations.each do |o| %>
		<p><%= button_to o.name, choose_organization_path(org_id: o.id) %></p>
	<% end %>
  </div>
</div>
