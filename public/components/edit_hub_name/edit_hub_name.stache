<div class="hub-name-wrap {{#if isEditing}}hub-name-is-editting{{/if}}">
	<b>Hub Name</b>
  {{#if isEditing}}
	  <input type="text" class="form-control hub-name" value="{{ hub.name }}">
  {{else}}
	  <h2>{{ hub.name }}</h2>
	  <i class="fa fa-pencil edit-hub-name"></i>
  {{/if}}
</div>

