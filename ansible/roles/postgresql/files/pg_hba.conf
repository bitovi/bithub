local   all             postgres                                ident map=usr2pg
local   all             all                                     trust

# localhost
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust

# localnets
host    all             all             10.0.0.0/8              md5
host    all             all             192.168.0.0/16          md5
