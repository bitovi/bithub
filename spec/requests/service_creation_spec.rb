require 'rails_helper'

RSpec.describe 'Service creation', type: :request do
  context 'creating a service based on Brand Identity' do
    it 'responds with service + config'
  end

  context 'creating a service all on it\'s own' do
    it 'responds with service + config all on its own'
  end
end
