require 'spec_helper'

describe "3rd party auth API" do
  it "authenticates the user through Twitter's API"
  it "authenticates the user through Github's API"
  it "authenticates the user through Meetup's API"
end
