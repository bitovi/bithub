require 'no_rails_spec_helper'
require 'services/crawler/crawler'

describe DigestSet do

  describe "#add_many" do
    it "adds many stuff to redis, and checks if everything was added"
  end
  
  describe "#key" do
    it "determines the key-path for a dispatched event"
  end
end
