require 'spec_helper'

describe User do

  context "upon creation" do
    
    describe "#sum_points" do
      it "calculates total points" do
        #user = crate(@user).sum_points
        #expect(user.points).to eq(sum)
      end
    end

  end
end
