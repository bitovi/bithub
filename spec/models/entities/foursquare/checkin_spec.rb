require 'rails_helper'

describe Entities::Foursquare::Checkin do
  describe 'data' do
    it 'prepares the data for building/updating' #do
      # checkin_event = Events::Foursquare::CheckinEvent.new(
      #   raw_data(response_path: 'foursquare/checkin_event')['response'][1])

      # entity_wrapper = Entities::Disqus::Post.new(checkin_event)

      # expect(entity_wrapper.data.keys).to include(
      #   :title, :body, :url, :origin_id, :origin_ts)

      # expect(entity_wrapper.data[:props].keys).to include(
      #   :origin_author_id, :origin_author_name,
      #   :venue_id, :venue_name)
    # end
  end
end
